server:
  port: 7070
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/YonyCJ/Example-Microservice-Spring-Boot-3.2.3.git
          #username:
          #password:
          search-paths: config-data
          default-label: master
  security:
    user:
      name: root
      password: s3cr3t
